<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Intermediate" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/grey</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.Intermediate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="customFloatingAdd" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="fabCustomSize">64dp</item>
        <item name="backgroundTint">@color/grey</item>
        <item name="srcCompat">@drawable/ic_add</item>
        <item name="tint">@color/white</item>
        <item name="maxImageSize">24dp</item>
    </style>

    <style name="customFloatingCamera" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="fabCustomSize">64dp</item>
        <item name="backgroundTint">@color/grey</item>
        <item name="srcCompat">@drawable/ic_camera</item>
        <item name="tint">@color/white</item>
        <item name="maxImageSize">24dp</item>
    </style>

    <style name="customFloatingPicture" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="fabCustomSize">64dp</item>
        <item name="backgroundTint">@color/grey</item>
        <item name="srcCompat">@drawable/ic_picture</item>
        <item name="tint">@color/white</item>
        <item name="maxImageSize">24dp</item>
    </style>
</resources>